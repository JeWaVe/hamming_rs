initSidebarItems({"fn":[["distance","Computes hamming distance Assumes x and y have same memory alignment Uses highly optimized avx2 version if available fallback on [distance_faster] if `x` and `y` have different alignment or if avx2 features are not available"],["distance_faster","Computes hamming distance Assumes `x` and `y` have same length slightly faster than naive version  "],["distance_naive","Computes hamming distance (naive version)"],["weight","Computes hamming weight Uses highly optimized avx2 version if available"],["weight_naive","computes hamming weight (naive version)"]],"mod":[["lib_avx2","avx2 target specific functions"],["utils","export utilies (such as cross platform aligned_alloc)"]]});